# TaskMaster - JavaScript To-Do App

A dynamic task management application built with Vanilla JavaScript. Unlike static websites, this application interacts with the browser's LocalStorage API to save user data, ensuring tasks persist even after the page is refreshed.

## 🚀 Live Demo
[View the Live Project Here](PASTE_YOUR_GITHUB_PAGES_LINK_HERE)

## 🛠 Tech Stack
* **JavaScript (ES6):** Logic for adding, deleting, and updating tasks.
* **LocalStorage API:** Handles data persistence (saving tasks to the browser's memory).
* **DOM Manipulation:** Dynamically creating and removing HTML elements based on user input.
* **CSS3:** Clean, modern UI styling.

## ✨ Key Features
* **Persistent Data:** Tasks are saved to LocalStorage, so they don't disappear on page refresh.
* **CRUD Functionality:** Users can Create (add), Read (view), and Delete tasks.
* **State Management:** Click tasks to toggle their "completed" status (strikethrough effect).
* **Input Validation:** Prevents adding empty tasks to the list.
